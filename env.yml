name: context-aware-llm
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10
  - pip=24.*
  - pytorch=2.5.*
  - pytorch-cuda=12.1
  - numpy
  - pandas
  - tqdm
  - pyyaml
  - regex
  - tensorboard
  - pip:
      - transformers>=4.51.0
      - accelerate>=0.34.0
      - datasets>=2.20.0
      - peft>=0.12.0
      - evaluate>=0.4.2
      - safetensors>=0.4.3
      - sentencepiece>=0.2.0
      - tokenizers>=0.20.0
      - bitsandbytes>=0.43.0; platform_system != "Windows"
